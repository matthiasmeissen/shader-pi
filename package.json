{
  "name": "shader-pi",
  "version": "1.0.0",
  "description": "Using the raspberry pi to run and control glsl shaders",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.2"
  }
}
